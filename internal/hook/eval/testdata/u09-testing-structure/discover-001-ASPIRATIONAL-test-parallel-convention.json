{
  "description": "ASPIRATIONAL U9 Discovery: engine should detect that project uses t.Parallel() in most tests and surface it as a convention",
  "hook": "quality-gate",
  "project_root": "REPO_ROOT",
  "input": {
    "tool_name": "Edit",
    "tool_input": {"file_path": "internal/hook/quality_gate_test.go", "new_string": "func TestNew(t *testing.T) {\n\t// no t.Parallel()\n\tresult := RunQualityGate(input)\n\tif result == nil { t.Fatal(\"nil\") }\n}"}
  },
  "expect": {
    "has_output": true,
    "contains": ["U9", "t.Parallel", "convention"],
    "max_latency_ms": 5000
  }
}
