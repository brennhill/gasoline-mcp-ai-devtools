{
  "description": "ASPIRATIONAL U9 Enforce: tests sharing mutable state via package-level var — should flag as test isolation violation",
  "hook": "quality-gate",
  "project_root": "REPO_ROOT",
  "input": {
    "tool_name": "Edit",
    "tool_input": {"file_path": "internal/hook/quality_gate_test.go", "new_string": "var testCounter int\n\nfunc TestA(t *testing.T) {\n\ttestCounter++\n\tif testCounter != 1 { t.Fatal(\"order dependent\") }\n}\n\nfunc TestB(t *testing.T) {\n\ttestCounter++\n\tif testCounter != 2 { t.Fatal(\"order dependent\") }\n}"}
  },
  "expect": {
    "has_output": true,
    "contains": ["U9", "shared state", "test isolation"],
    "max_latency_ms": 5000
  }
}
