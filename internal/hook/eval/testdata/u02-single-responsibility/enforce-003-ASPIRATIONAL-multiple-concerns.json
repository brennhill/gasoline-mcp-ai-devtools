{
  "description": "ASPIRATIONAL U2 Enforce: file mixes HTTP handling with database access — should flag multiple concerns in one file",
  "hook": "quality-gate",
  "project_root": "REPO_ROOT",
  "input": {
    "tool_name": "Edit",
    "tool_input": {"file_path": "internal/hook/protocol.go", "new_string": "func HandleRequest(w http.ResponseWriter, r *http.Request) {\n\trows, _ := db.Query(\"SELECT * FROM users\")\n\tjson.NewEncoder(w).Encode(rows)\n}"}
  },
  "expect": {
    "has_output": true,
    "contains": ["U2", "multiple concerns", "separate"],
    "max_latency_ms": 5000
  }
}
