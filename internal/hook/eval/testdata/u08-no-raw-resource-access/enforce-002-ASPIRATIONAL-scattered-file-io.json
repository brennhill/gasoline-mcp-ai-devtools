{
  "description": "ASPIRATIONAL U8 Enforce: os.ReadFile scattered across 20+ files instead of through a helper — should flag the pattern as needing consolidation",
  "hook": "quality-gate",
  "project_root": "REPO_ROOT",
  "input": {
    "tool_name": "Edit",
    "tool_input": {"file_path": "internal/hook/protocol.go", "new_string": "data, err := os.ReadFile(configPath)\nif err != nil {\n\treturn nil, err\n}"}
  },
  "expect": {
    "has_output": true,
    "contains": ["U8", "os.ReadFile", "consolidate", "helper"],
    "max_latency_ms": 5000
  }
}
