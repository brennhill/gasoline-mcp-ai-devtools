{
  "description": "ASPIRATIONAL U6 Suggest: nil check deep in call chain — should suggest validating at the entry point instead",
  "hook": "quality-gate",
  "project_root": "REPO_ROOT",
  "input": {
    "tool_name": "Edit",
    "tool_input": {"file_path": "internal/hook/protocol.go", "new_string": "func innerHelper(data *Config) string {\n\tif data == nil {\n\t\treturn \"\"\n\t}\n\treturn data.Name\n}"}
  },
  "expect": {
    "has_output": true,
    "contains": ["U6", "validate", "entry point", "nil"],
    "max_latency_ms": 5000
  }
}
