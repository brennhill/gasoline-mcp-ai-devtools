{
  "description": "ASPIRATIONAL U6 Enforce: silently returning a default value instead of erroring on invalid input — should flag",
  "hook": "quality-gate",
  "project_root": "REPO_ROOT",
  "input": {
    "tool_name": "Edit",
    "tool_input": {"file_path": "internal/hook/protocol.go", "new_string": "func getLimit(cfg map[string]int) int {\n\tv, ok := cfg[\"limit\"]\n\tif !ok {\n\t\treturn 100\n\t}\n\treturn v\n}"}
  },
  "expect": {
    "has_output": true,
    "contains": ["U6", "silent default", "error"],
    "max_latency_ms": 5000
  }
}
