{
  "description": "ASPIRATIONAL U4 Enforce: introducing a new mutable package-level var should be flagged — use dependency injection instead",
  "hook": "quality-gate",
  "project_root": "REPO_ROOT",
  "input": {
    "tool_name": "Edit",
    "tool_input": {"file_path": "internal/hook/protocol.go", "new_string": "var activeConnections = make(map[string]*Connection)\n\nfunc AddConnection(id string, c *Connection) {\n\tactiveConnections[id] = c\n}"}
  },
  "expect": {
    "has_output": true,
    "contains": ["U4", "global", "inject"],
    "max_latency_ms": 5000
  }
}
