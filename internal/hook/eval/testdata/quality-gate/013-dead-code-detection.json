{
  "description": "ASPIRATIONAL: Edit introduces commented-out code block — should flag dead code",
  "hook": "quality-gate",
  "project_root": "REPO_ROOT",
  "input": {
    "tool_name": "Edit",
    "tool_input": {"file_path": "internal/hook/protocol.go", "new_string": "func NewHandler() {\n\t// oldImpl := buildOldHandler()\n\t// result := oldImpl.Process()\n\t// return result\n\tnewImpl := buildNewHandler()\n\treturn newImpl.Process()\n}"}
  },
  "expect": {
    "has_output": true,
    "contains": ["commented", "dead code", "remove"],
    "max_latency_ms": 500
  }
}
