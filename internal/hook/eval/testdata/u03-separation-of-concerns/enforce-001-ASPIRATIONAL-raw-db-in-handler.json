{
  "description": "ASPIRATIONAL U3 Enforce: raw database call in a handler file — should flag as separation of concerns violation",
  "hook": "quality-gate",
  "project_root": "REPO_ROOT",
  "input": {
    "tool_name": "Edit",
    "tool_input": {"file_path": "internal/server/main_response.go", "new_string": "rows, err := db.Query(\"SELECT * FROM sessions WHERE id = ?\", id)"}
  },
  "expect": {
    "has_output": true,
    "contains": ["U3", "data access", "handler"],
    "max_latency_ms": 5000
  }
}
