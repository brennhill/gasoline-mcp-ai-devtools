{
  "name": "gasoline-e2e-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "server:build": "cd .. && make dev"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "ws": "^8.19.0"
  }
}
