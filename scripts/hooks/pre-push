#!/bin/bash
# Pre-push hook for Kaboom
# Runs comprehensive CI checks before allowing push

set -e

echo "[Kaboom] Running pre-push CI checks..."
echo ""

# Run the full local CI suite
make ci-local

echo ""
echo "[Kaboom] All pre-push checks passed"
