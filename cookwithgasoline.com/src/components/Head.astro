---
// Head.astro — Global head injection for analytics
import Analytics from './Analytics.astro';

const siteBase = Astro.site?.toString() ?? 'https://cookwithgasoline.com';
const path = Astro.url?.pathname ?? '/';
const normalizedPath = path === '/' ? 'index' : path.replace(/^\/|\/$/g, '') || 'index';
const markdownUrl = new URL(`/markdown/${normalizedPath}.md`, siteBase).toString();
---

<meta name="robots" content="index,follow" />
<meta name="content-signal" content="ai-train=yes, search=yes, ai-input=yes" />
<link rel="alternate" type="text/markdown" href={markdownUrl} />
<Analytics />
