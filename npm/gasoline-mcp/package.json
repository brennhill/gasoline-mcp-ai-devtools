{
  "name": "gasoline-mcp",
  "version": "6.0.3",
  "description": "Gasoline MCP is an AI Debugger, Inspector, and Observability toolkit. It acts as Chrome DevTools for AI Agents using the Model Context Protocol (MCP).",
  "license": "AGPL-3.0-only",
  "bin": {
    "gasoline-mcp": "bin/gasoline-mcp"
  },
  "scripts": {
    "preinstall": "node -e \"const cp = require('child_process'); try { cp.execSync('npm uninstall -g gasoline-mcp', {stdio:'ignore'}); } catch(e) {} try { const cmd = process.platform === 'win32' ? 'taskkill /F /IM gasoline.exe 2>nul || true' : 'pkill -9 -x gasoline 2>/dev/null || true'; cp.execSync(cmd, {stdio:'ignore', shell:true}); } catch(e) {}\"",
    "postinstall": "node lib/postinstall-skills.js",
    "preuninstall": "node -e \"const cp = require('child_process'); try { const cmd = process.platform === 'win32' ? 'taskkill /F /IM gasoline.exe 2>nul || true' : 'pkill -9 -x gasoline 2>/dev/null || true'; cp.execSync(cmd, {stdio:'ignore', shell:true}); } catch(e) {}\""
  },
  "files": [
    "bin",
    "lib",
    "skills"
  ],
  "optionalDependencies": {
    "@brennhill/gasoline-darwin-arm64": "6.0.3",
    "@brennhill/gasoline-darwin-x64": "6.0.3",
    "@brennhill/gasoline-linux-arm64": "6.0.3",
    "@brennhill/gasoline-linux-x64": "6.0.3",
    "@brennhill/gasoline-win32-x64": "6.0.3"
  },
  "keywords": [
    "mcp",
    "mcp-server",
    "ai-debugging",
    "ai-observability",
    "llm-ops",
    "devtools",
    "prompt-engineering",
    "agent-inspection"
  ],
  "homepage": "https://cookwithgasoline.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/brennhill/gasoline-mcp-ai-devtools"
  },
  "bugs": {
    "url": "https://github.com/brennhill/gasoline-mcp-ai-devtools/issues"
  },
  "engines": {
    "node": ">=16"
  }
}
